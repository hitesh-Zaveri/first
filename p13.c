#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <map>
using namespace std;


int main() {
    map<string,double>dict=new hashmap<string,double>();
    int n;
    cin>>n;
    string *s=null;
    s=new string (n);
    doule *d=0;
    d=new double (n);
    for(int i=0;i<n;i++)
    {
        dict.get_allocator(s[i],)
    }
    vector<string>a();
    int i=0;
    while(getch!='\n')
    {
        cin>>a[i];
        i++;
    }

    return 0;
}
